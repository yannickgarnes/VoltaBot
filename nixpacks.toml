[phases.setup]
nixPkgs = ["chromium", "chromedriver", "python310"]

[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
# Localizamos chromium dinámicamente y lo pasamos al bot
cmd = "export CHROME_BIN=$(which chromium) && python bot.py"
