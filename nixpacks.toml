[phases.setup]
nixPkgs = ["chromium", "chromedriver", "python311"]

[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "export CHROME_BIN=$(which chromium) && PATH=$PATH:$(dirname $CHROME_BIN) python bot.py"
